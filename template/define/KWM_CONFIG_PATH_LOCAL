Controls where cluster assets (like public key infrastructure and encryption
keys) will be generated, and, when securely copied to Nodes during
bootstrapping, where it will be sourced from.

Examples:
  # See how this value affects cluster asset generation.
  KWM_CONFIG_PATH_LOCAL=test $SCRIPT_NAME render assets
