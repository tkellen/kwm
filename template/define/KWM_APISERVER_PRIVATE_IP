Kubelets communicate with kube-apiserver using this value.

See \$KWM_CONFIG_PATH_REMOTE/kubelet.kubeconfig on any worker node.

# TODO: ensure there is HA support for this.
# See how this variable is used in defining configuration for kubectl.
KWM_CLUSTER_NAME=test $SCRIPT_NAME render cluster-admin
KWM_CLUSTER_NAME=test $SCRIPT_NAME render cluster-admin | grep test
