Usage:
  kwm context [all cni controlplane dns etcd kubectl-root pki worker]

Description:
  Display the needed context to generate the specified resource.

Examples:
  To use the output of this command as the environment for another, try this:

    env -S "\$(kwm context pki)" kwm script
