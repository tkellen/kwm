Usage:
  kwm [command]

Available Commands:
  context   compile environment variables needed to render a script or manifest
  go        compile a script to bootstrap a cluster in one command
  help      show information about a provided command
  info      show information about this software
  manifest  compile a yaml manifest from environment variables
  script    compile a bash script from environment variables
